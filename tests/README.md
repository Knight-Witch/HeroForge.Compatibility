# Tests

Planned test areas:

```text
tests/
  fixtures/
  compatibility/
  reports/
```

Use this area for:

- retained HeroForge bundle/build fixtures where legally and practically appropriate,
- patch match-count and transformation tests,
- capability compatibility tests,
- automated regression checks,
- stored test reports.

A fixture passing does not prove live runtime compatibility.