# Maintained Source

No maintained runtime source has been implemented yet.

Planned boundaries:

- `bridge/` — capability registry, adapters, discovery, readiness.
- `patch-engine/` — shared interception, patch definitions, validation, fallback.
- `features/` — coherent domain feature modules.
- `shared/` — project-level utilities that are not HeroForge-specific feature behavior.

Do not place immutable legacy originals here.