# Manifests

Reserved for future machine-readable project metadata such as:

- feature registry data,
- compatibility status,
- ownership metadata,
- optional data-only emergency disable configuration.

Do not add remote executable-code control here.

Any future public compatibility manifest must be separately designed and reviewed before use.